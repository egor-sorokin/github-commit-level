# API Endpoints

## `/stats`
##### ENDPOINT
`/stats`
##### METHOD
`GET`

##### RESPONSE
A JSON object. The `data` key refers to an object containing `username`, `commits`, `nextLevel`, `currentLevel`.

